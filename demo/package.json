{
  "name": "znak-demo",
  "devDependencies": {
    "@types/bun": "^1.1.12",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "build": "bun run scripts/build.ts",
    "serve": "bun run build && bunx serve -n dist",
    "dev": "bun run scripts/dev.ts"
  },
  "type": "module"
}
