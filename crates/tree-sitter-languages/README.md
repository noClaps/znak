A collection of Tree-sitter grammars and queries.

This crate is mainly to be used with [Highlight](../highlight/index.html), but it can be used on its own for other applications. The language modules listed below are not included with this crate by default, and must be enabled as features individually. To enable all languages, use the `all` feature.
